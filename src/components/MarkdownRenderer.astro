---
import MarkdownIt from "markdown-it";

const { content } = Astro.props;

const md = new MarkdownIt();
const html = content ? md.render(content) : "";
---

<section class="prose prose-neutral max-w-none" set:html={html} />
